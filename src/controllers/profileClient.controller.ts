import { Request, Response, NextFunction } from "express";
import { ProfilePostRequest } from "../dtos/profileClient.dto";

// async function post(req: Request<{}, {}, ProfilePostRequest>, res: Response, next: NextFunction) {
//   try {
//     const parsedData = profileCl
//   } catch (error) {
//     next(error)
//   }
// }

const profileClientController = {
  // post,
};

export default profileClientController;
